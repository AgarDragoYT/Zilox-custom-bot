const Discord = require("discord.js");
let color = ((1 << 24) * Math.random() | 0).toString(16);
module.exports.run = async (bot, message, args) => {
    let hEmbed = new Discord.RichEmbed()
    .addField(":house: Main Commands :house", "r")
}

module.exports.help = {
name: "aliases",
aliases: ["a", "alias"]
}
